# AGENTS.md

This repository's agent guidance lives in [`CLAUDE.md`](CLAUDE.md) — quick commands, the
architecture rules that must not break, and the gotchas that live nowhere else. It is written for
any agent tool, not only Claude.

Prefer changing `docs/` and letting this file and `CLAUDE.md` point at it, so the two cannot drift
apart.
