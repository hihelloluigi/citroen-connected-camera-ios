//
//  Secrets.example.xcconfig — the shape of the generated Configurations/Secrets.xcconfig.
//
//  Secrets.xcconfig is written by `ENV_NAME=<env> ./scripts/generate_secrets.sh` from
//  `envs/.env.<env>`, and is git-ignored. Never edit it by hand and never commit it; this
//  file exists only so a reader can see which keys end up there.
//

CCAM_TEAM_ID = ABCDE12345
